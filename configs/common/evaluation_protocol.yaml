evaluation:
  # Supported modes: protocol, oracle_full_update.
  mode: protocol
  # Forecasting metrics currently support mse, mae, mape, cross_entropy; tasks also append the primary metric automatically.
  metrics: [mse, mae, mape]
