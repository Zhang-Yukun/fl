includes:
  - common/forecasting_patchtst.yaml
  - common/federated.yaml

experiment:
  name: soteriafl
  mode: federated
  output_dir: outputs/soteriafl

federated:
  algorithm: soteriafl
  topk_fraction: 0.10

evaluation:
  mode: protocol

privacy:
  clip_norm: 2.0
  noise_multiplier: 0.01
