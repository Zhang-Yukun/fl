includes:
  - common/rawdata2_patchtst_base.yaml
  - common/federated_sync_300.yaml

experiment:
  name: rawdata2_dp_topk
  mode: federated
  output_dir: outputs/rawdata2_dp_topk

federated:
  algorithm: dp_topk_fedavg
  topk_fraction: 0.50

evaluation:
  mode: oracle_full_update

privacy:
  clip_norm: 2.0
  noise_multiplier: 0.002
