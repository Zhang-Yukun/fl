includes:
  - common/forecasting_patchtst.yaml
  - common/federated.yaml

experiment:
  name: fedaware
  mode: federated
  output_dir: outputs/fedaware

federated:
  algorithm: fedaware

fedaware:
  alpha: 0.5
  steps: 50
  lr: 0.1
