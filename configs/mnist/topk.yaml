includes:
  - base_federated.yaml

experiment:
  name: mnist_topk_classification
  mode: federated
  output_dir: outputs/mnist_topk_classification

federated:
  algorithm: sparse_fedavg
  rounds: 5
  topk_fraction: 0.10
