includes:
  - common/rawdata2_patchtst_base.yaml
  - common/federated_sync_300.yaml

experiment:
  name: rawdata2_sign
  mode: federated
  output_dir: outputs/rawdata2_sign

federated:
  algorithm: sign_fedavg

evaluation:
  mode: oracle_full_update
