includes:
  - common/forecasting_patchtst.yaml
  - common/federated.yaml

experiment:
  name: dp_topk
  mode: federated
  output_dir: outputs/dp_topk

federated:
  algorithm: dp_topk_fedavg
  topk_fraction: 0.50

evaluation:
  mode: protocol

privacy:
  clip_norm: 2.0
  noise_multiplier: 0.002
