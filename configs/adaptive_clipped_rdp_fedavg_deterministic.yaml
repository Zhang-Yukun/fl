includes:
  - common/runtime_deterministic_single_thread.yaml
  - adaptive_clipped_rdp_fedavg.yaml

experiment:
  name: adaptive_clipped_rdp_fedavg_deterministic
  output_dir: outputs/adaptive_clipped_rdp_fedavg_deterministic

data:
  shuffle_train: false

model:
  dropout: 0.0

attack:
  seed: 2026
  async_enabled: false

tracking:
  enabled: true
  offline: true

adaptive_clipped_rdp:
  seed: 2026
