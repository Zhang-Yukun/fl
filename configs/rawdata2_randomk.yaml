includes:
  - common/rawdata2_patchtst_base.yaml
  - common/federated_sync_300.yaml

experiment:
  name: rawdata2_randomk
  mode: federated
  output_dir: outputs/rawdata2_randomk

federated:
  algorithm: randomk_fedavg
  topk_fraction: 0.10
  randomk_seed: 2026
