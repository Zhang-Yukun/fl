includes:
  - common/forecasting_patchtst.yaml
  - common/federated.yaml

experiment:
  name: sign
  mode: federated
  output_dir: outputs/sign

federated:
  algorithm: sign_fedavg

evaluation:
  mode: protocol
