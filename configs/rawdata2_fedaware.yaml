includes:
  - common/rawdata2_patchtst_base.yaml
  - common/federated_sync_300.yaml

experiment:
  name: rawdata2_fedaware
  mode: federated
  output_dir: outputs/rawdata2_fedaware

federated:
  algorithm: fedaware

fedaware:
  alpha: 0.5
  steps: 50
  lr: 0.1
